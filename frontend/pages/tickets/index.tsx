// TicketsPage.js
import React from 'react';
import TicketTable from '../../components/TicketTable'; // Adjust the path accordingly

const TicketPage = () => {
    return (
        <div style={{ marginTop: '20px', marginBottom: '20px' }}>
            <TicketTable />
        </div>
    );
};

export default TicketPage;
